# CPU-only extras for GraphSAGE training (Python side).
# Native C++ / graph_sampler does not depend on these packages.
#
# Install (preferred CPU wheel index for PyTorch, PyPI for PyG):
#   python3 -m pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cpu
#   python3 -m pip install torch-geometric==2.6.1
#
# Or:
#   python3 -m pip install -r python/requirements-train.txt \
#     --extra-index-url https://download.pytorch.org/whl/cpu
#
# torch-geometric>=2.3 runs mean SAGEConv with only PyTorch. Do not add
# optional extension wheels (torch-scatter, torch-sparse, pyg-lib) to default
# CI. Neighborhoods still come from native graph_sampler via
# sagerec_minibatch; do not install or use a PyG NeighborLoader as the
# sampling path. Do not add CUDA wheels to default CI.
torch==2.6.0
torch-geometric==2.6.1
